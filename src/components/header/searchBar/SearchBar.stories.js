import SearchBarComponent from './SearchBar';

export default {
  title: 'SearchBar',
  component: SearchBarComponent,
};

const Template = () => <SearchBarComponent />;

export const SearchBar = Template.bind({});
